示例 Example ⬇️

```jsx
<div>
{
  /*
  请求
  const reg = new RegUtil();
  // 获取全部验证规则集
  reg.getRule();

  // 获取单个验证规则集
  reg.getRule("mobile");

  // 校验规则
  reg.checkRule([{type: "date", value: "2018-09-30"}]);

  // 设置验证规则并校验
  reg.setRule([{type: "email", value: /^\w{0,}$/}]).checkRule([{type: "email", value: "1@qq.com"}])
  */
}
</div>
```
